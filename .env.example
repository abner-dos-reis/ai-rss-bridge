# Optional environment variables for AI RSS Bridge

# Flask settings
FLASK_ENV=production
FLASK_DEBUG=false

# Default AI provider (optional)
DEFAULT_AI_PROVIDER=openai

# Database settings (optional - uses SQLite by default)
DATABASE_PATH=/app/data/feeds.db

# Update interval in hours (default: 1)
UPDATE_INTERVAL_HOURS=1

# Log level (default: INFO)
LOG_LEVEL=INFO